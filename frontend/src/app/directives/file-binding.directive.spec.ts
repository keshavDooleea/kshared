import { FileBindingDirective } from './file-binding.directive';

describe('FileBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new FileBindingDirective();
    expect(directive).toBeTruthy();
  });
});
